name: Screenshots

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  screenshots:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[skip ci]') && github.actor != 'github-actions[bot]'"
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref || github.ref }}
      - uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
      - uses: android-actions/setup-android@v3
      - uses: gradle/actions/setup-gradle@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      # updateScreenshots rebuilds every image the README links to: scripts/render_device_frames.py
      # draws the macOS/browser window frames for media/screen_mac_*.png and media/screen_web_*.png,
      # and updateEnUsStoreScreenshots refreshes fastlane/metadata/android/en-US/images/. The other
      # 19 locales stay a manual ':screenshotTests:generateStoreScreenshots' before a store release.
      - run: pip install pillow
      - run: ./gradlew spotlessApply
      - run: ./gradlew :screenshotTests:updateScreenshots
      - name: Auto-commit if changed
        if: github.event_name == 'push'
        run: |
          git diff --quiet && exit 0
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Auto-fix: spotlessApply and updateScreenshots [skip ci]"
          git push
