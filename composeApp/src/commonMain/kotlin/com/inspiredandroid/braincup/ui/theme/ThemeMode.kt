package com.inspiredandroid.braincup.ui.theme

/** User-selectable app theme. SYSTEM follows the OS (and Material You on Android). */
enum class ThemeMode {
    SYSTEM,
    LIGHT,
    DARK,
    OLED,
}
